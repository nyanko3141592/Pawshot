import AppKit

// Text tool implementation is handled in EditorState.beginAnnotation (text prompt)
// and AnnotationRenderer.drawText (rendering).
//
// When the text tool is selected, clicking on the canvas prompts the user
// to enter text via an NSAlert with a text field.
